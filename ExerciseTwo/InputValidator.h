#ifndef InputValidator_H
#define InputValidator_H

#include "Wand.h"
#include <list>
#include <string>

int GetValidInteger(std::string message);
std::string GetValidString(std::string message);

#endif